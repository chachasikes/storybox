export default function modelLoadedEvent() {
  // let animations = document.querySelectorAll(".glb-animation");
  // // Update all animations
  // animations.forEach(animation => {
  //   let autoplay = animation.getAttribute('data-autoplay');
  //   if (autoplay === 'true') {
  //     // if (animation.components !== undefined &&
  //     // animation.components["animation-mixer-storybox"] !== undefined &&
  //     // animation.components["animation-mixer-storybox"].activeActions !== undefined) {
  //     //   console.log(animation.components["animation-mixer-storybox"]);
  //     //   // Make animation stay at final frame.
  //     //   // animation.components["animation-mixer-storybox"].data.clampWhenFinished = true;
  //     // }
  //   }
  // });
}
